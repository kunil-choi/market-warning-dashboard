# 파일 생성
cat > backend/requirements.txt << 'EOF'
yfinance>=0.2.36
pandas>=2.0.0
numpy>=1.26.0
fredapi>=0.5.1
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
requests>=2.32.0
httpx>=0.27.0
EOF

# 설치
pip install -r backend/requirements.txt

# 설치 확인
pip list | grep -E "yfinance|fredapi|fastapi"
